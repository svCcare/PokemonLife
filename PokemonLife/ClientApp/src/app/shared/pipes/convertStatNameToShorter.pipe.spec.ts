/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ConvertStatNameToShorterPipe } from './convert-stat-name-to-shorter.pipe';

describe('Pipe: ConvertStatNameToShortere', () => {
  it('create an instance', () => {
    let pipe = new ConvertStatNameToShorterPipe();
    expect(pipe).toBeTruthy();
  });
});
